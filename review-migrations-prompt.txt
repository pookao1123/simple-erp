You are claude-codereview. Review the Phase 3 migration SQL files.

Work in D:\VS\Project\Simple_ERP.
Migration files:
- migrations/001_create_customers.sql
- migrations/002_create_products.sql
- migrations/003_create_invoices.sql
- migrations/004_create_line_items.sql
- migrations/005_seed_data.sql

Design doc (approved): D:\VS\MD_obsidian\Hermes\design\phase-3-database-schema.md
Previous review report: D:\VS\MD_obsidian\Hermes\job\review-phase-3-database-schema-design.md

Read all 5 migration files. Then write a review report to:
D:\VS\MD_obsidian\Hermes\job\review-phase-3-migrations-code.md

Review criteria:

1. **Correctness — does each migration match the approved design doc?**
   - Table columns: correct names, types, constraints, defaults vs design doc §4
   - Indexes: all required indexes present (see design doc §4 for each table)
   - RLS policies: correct policies for each table (SELECT/INSERT/UPDATE/DELETE, own rows + admin bypass)
   - Triggers: `updated_at` trigger on every table (shared function + per-table trigger)
   - Invoice UPDATE policy: restricted to `status = 'draft'` only
   - Line items RLS: EXISTS check on invoices table for ownership (not direct user_id column)
   - `invoice_totals` view: GROUP BY includes ALL non-aggregated columns (this was a critical finding in the design review — verify it's fixed)

2. **SQL syntax — will these run without errors on PostgreSQL?**
   - CREATE TABLE IF NOT EXISTS — correct syntax
   - CREATE INDEX IF NOT EXISTS — correct syntax
   - DROP POLICY IF EXISTS + CREATE POLICY — correct order, no conflicts
   - FK references: customers before invoices, products before line_items — correct migration order
   - ON DELETE actions: customers→invoices = RESTRICT, products→line_items = SET NULL, invoices→line_items = CASCADE
   - CHECK constraints: valid syntax, correct conditions
   - GENERATED ALWAYS AS: correct syntax for `amount` column
   - DO $$ ... $$ block for seed data: correct PL/pgSQL syntax

3. **Seed data — is it correct and complete?**
   - 5 customers, 10 products, 20 invoices (5 draft + 8 sent + 5 paid + 2 overdue)
   - UUID placeholder: is the placeholder clearly marked? `00000000-0000-0000-0000-000000000000`
   - Seed script raises an error if the placeholder UUID is not replaced (the DO $$ block checks this)
   - Invoice numbers: INV-2026-0001 through INV-2026-0020 — sequential, no gaps
   - Line items reference correct product_ids (1-10) and invoice_ids (matched by invoice_no)
   - Multi-line invoices: INV-2026-0006, INV-2026-0007, INV-2026-0017, INV-2026-0016 have 2-3 lines
   - Status mix: 5 draft, 8 sent, 5 paid, 2 overdue — correct?
   - Dates: within 2026-03-01 to 2026-09-25 range — correct?
   - All line items reference valid product IDs (1-10) that exist in the seed

4. **RLS security — any gaps?**
   - Are there any tables with RLS enabled but missing a policy (e.g. SELECT policy missing = no one can read)?
   - Does the admin bypass work correctly? `EXISTS (SELECT 1 FROM user_roles WHERE user_roles.user_id = auth.uid() AND user_roles.role = 'admin')`
   - Line items: does the SELECT policy correctly gate on invoice ownership via EXISTS subquery?
   - Are there any policy conflicts (two policies that contradict each other)?
   - Note: the design doc calls out that `user_roles` RLS is handled in Phase 2 (already done) — don't re-review that here, but verify the migration files don't re-create `user_roles` policies incorrectly

5. **Edge cases — anything missing?**
   - Soft-delete: is `active` column present on customers and products? Are RLS policies filtering on `active = true`? (Design doc uses soft-delete — verify RLS policies filter inactive rows, or note if they don't)
   - Invoice `total` is stored — is there any trigger or generated column that auto-recalculates it? (Design doc says Phase 5 backend recalculates — verify no auto-calc exists here, which would be wrong)
   - `invoice_no` uniqueness: UNIQUE(user_id, invoice_no) — correct?
   - `updated_at` trigger: does it fire on every UPDATE? Does it handle the case where `updated_at` is explicitly set (e.g. in seed data)? (Trigger only fires on UPDATE, not INSERT — INSERT uses DEFAULT now() — correct)

6. **Migration file hygiene**
   - Are comments clear and useful?
   - Is the seed script's UUID replacement instruction prominent and clear?
   - Any dangerous operations (DROP TABLE, TRUNCATE, etc.) that shouldn't be here?

Report format:
- Summary: 2-3 sentences on migration file quality
- Findings table: | # | Severity | File | Finding | Recommendation |
- Each finding specific (reference file + line or section)
- Severity: critical (blocks apply), major (should fix before merge), minor (nice to fix), cosmetic
- At the end: approval status — ready to apply to Supabase / needs fixes / rejected

No false positives. If something is correct, say so and why.
